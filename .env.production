# Production Environment Variables
# API URL points to the same domain (Cloudflare Pages Functions)
# Leave VITE_API_URL unset so Vite embeds '' and the frontend uses relative URLs (same-origin Cloudflare Pages)
# VITE_API_URL=
